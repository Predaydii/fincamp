services:
  - type: web
    name: fincamp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fincamp-db
          property: connectionString

databases:
  - name: fincamp-db
    plan: free
